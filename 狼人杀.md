# 狼人杀全链路项目 - 产品需求文档（当前实现同步版）

**版本：** V2.3  
**更新日期：** 2026-04-08  
**适用仓库：** `backend` / `admin` / `miniprogram-vue`

## 更新说明

本文档已根据当前仓库已落地功能同步更新，重点补充了以下变化：

- 小程序端已统一为 `uni-app + Vue 3 + Vite`，不再维护旧版原生小程序方案
- 后端已切换为 `Spring Boot + MyBatis-Plus + MySQL 8` 的真实持久化方案
- 板子列表卡片、收藏交互、笔记创建方式、微信资料完善、后台板子配置字段均已按当前实现同步
- 板子新增独立字段 `card_description`，用于板子列表卡片和详情概览的文案展示
- 角色分类模型固定为 `阵营 + 角色类型` 双维度，不再允许后台自由发散录入

## 一、产品概述

### 1.1 产品目标

为狼人杀玩家提供一个集 `板子图鉴 + 角色图鉴 + 面杀笔记 + 社区交流 + 后台治理` 于一体的完整系统。

系统目标：

- 帮用户快速浏览不同板子的人数、阵容、规则和角色配置
- 帮用户在面杀过程中快速记录发言、投票、查验和狼坑信息
- 帮用户发布复盘、提问和板型讨论内容
- 帮运营或管理员统一维护板子、角色、帖子、评论和举报数据

### 1.2 终端组成

| 终端 | 目录 | 职责 |
| --- | --- | --- |
| 小程序端 | `miniprogram-vue` | 板子浏览、角色图鉴、笔记、社区、用户中心 |
| 管理后台 | `admin` | 板子管理、角色管理、社区治理、图片上传 |
| 后端服务 | `backend` | 数据存储、接口服务、上传、鉴权、社区治理能力 |

### 1.3 当前版本状态

| 模块 | 状态 | 说明 |
| --- | --- | --- |
| 板子库 | 已实现 | 列表、筛选、搜索、收藏、详情、概览、规则、FAQ |
| 角色图鉴 | 已实现 | 分阵营列表、详情、相关板子 |
| 笔记本 | 已实现 | 本地存储、双模式建局、事件流、投票模板 |
| 用户系统 | 已实现 | 微信登录、资料完善、收藏、本地数据管理 |
| 社区系统 | 已实现 | 帖子、评论、点赞、举报、后台治理 |
| 管理后台 | 已实现 | 黑金风格控制台，支持板子/角色/社区治理 |
| AI 助手 | 预留 | 本期不实现 |

## 二、技术栈与设计原则

### 2.1 技术栈

- 后端：Java 21 + Spring Boot 3.5 + MyBatis-Plus + MySQL 8 + SpringDoc OpenAPI
- 小程序端：uni-app + Vue 3 + Vite，构建目标为微信小程序
- 管理后台：Vue 3 + Element Plus

### 2.2 设计原则

- 视觉基线遵循 `D:\Ai\wolfbook\awesome-design-md-main\design-md\lamborghini\DESIGN.md`
- 统一使用黑金高对比风格：纯黑背景、金色主操作、弱边框、强层级
- Web 端、小程序端、后台端保持统一的状态语义和品牌气质
- 数据以 MySQL 为唯一事实源；仅 `收藏` 和 `笔记` 保留本地存储

### 2.3 固定约束

- 小程序固定 4 个 Tab：`板子 / 社区 / 笔记 / 我的`
- 角色阵营固定：`好人 / 狼人 / 第三方`
- 角色类型固定：`平民 / 神职 / 狼人 / 功能狼 / 第三方`
- 本地存储 key 固定：
  - `auth_token`
  - `user_profile`
  - `favorite_boards`
  - `werewolf_sessions`

## 三、小程序端功能需求

### 3.1 Tab 结构

| Tab | 名称 | 页面 | 说明 |
| --- | --- | --- | --- |
| 1 | 板子 | `pages/boards/list` | 板子列表与角色图鉴入口 |
| 2 | 社区 | `pages/community/index` | 帖子流、发帖、详情 |
| 3 | 笔记 | `pages/sessions/index` | 本地对局记录与复盘 |
| 4 | 我的 | `pages/user/index` | 登录、收藏、设置、资料完善 |

### 3.2 板子模块

#### 3.2.1 板子列表页

- 页面顶部：
  - 左侧标题为 `板子库`
  - 右侧为 `角色` 按钮，点击进入角色图鉴页
  - 标题下方保留一句产品级引导文案
- 搜索与筛选：
  - 搜索框支持按板子名称模糊搜索
  - 搜索框右侧提供独立 `搜索` 按钮
  - 筛选项当前包括：
    - 人数：`全部 / 9 / 12 / 15+`
    - 难度：`入门 / 进阶 / 烧脑`
    - 标签：`经典 / 教学 / 娱乐 / 高配 / 第三方`
- 板子卡片采用纵向大图结构，包含：
  - 顶部封面图
  - 封面图右上角爱心收藏按钮，区分收藏与未收藏状态
  - 卡片内容区展示：
    - 难度标签
    - 当前搜索结果数文案
    - 板子名称
    - 右侧人数徽标
    - 角色头像标签组
    - 卡片底部一句板子介绍
- 卡片中的角色标签只保留 `图标 + 角色名/数量` 的形式，不再展示单独一整行纯文字阵容摘要
- 角色标签需支持显示平民数量，例如：`4平民`
- 板子卡片底部介绍文案来源优先级：
  - 后台配置的 `cardDescription`
  - 若未配置，则回退到后端生成的 `summary`
- 点击卡片进入板子详情页
- 支持下拉刷新

#### 3.2.2 收藏交互

- 收藏入口统一放在板子列表卡片封面右上角的爱心按钮
- 收藏数据本地存储在 `favorite_boards`
- 收藏后：
  - 爱心变为高亮状态
  - “我的收藏”页可立即看到对应板子
- 取消收藏后：
  - 爱心恢复未选中状态
  - “我的收藏”页同步移除
- 板子详情页不再保留底部大按钮式收藏入口

#### 3.2.3 我的收藏页

- 收藏页复用板子列表完全一致的卡片样式
- 收藏页卡片同样支持：
  - 封面图右上角爱心取消收藏
  - 人数徽标
  - 角色头像标签组
  - 板子介绍文案
- 收藏页空态需要提示用户去板子列表点亮爱心

#### 3.2.4 板子详情页

- 顶部为沉浸式头图区域，展示：
  - 封面图
  - 人数、难度、规则类型、胜利条件
- “板型概览”区域优先展示后台配置的板子介绍，展示优先级为：
  - `cardDescription`
  - `summary`
  - `lineupSummary`
  - `campSummary`
  - `briefConfig`
- “角色配置”区域采用网格形式展示角色头像、名称和数量
- “规则详情”展示 `specialRules`
- “小贴士”展示 `tips`
- “常见问题”展示 `faqs`

#### 3.2.5 角色图鉴页

- 分组展示三个阵营：
  - 好人
  - 狼人
  - 第三方
- 每条角色展示：
  - 头像
  - 名称
  - 阵营说明 `camp`
- 点击进入角色详情页

#### 3.2.6 角色详情页

- 展示角色立绘或头像
- 展示：
  - 角色名
  - 别名
  - 阵营
  - 角色类型
  - camp 文案
  - 技能说明
  - 背景说明
  - FAQ
- 若后端返回相关板子，则展示“出现过的板子”横向列表，并支持跳转板子详情

### 3.3 笔记本模块

笔记本保持纯本地存储，不回写后端。

#### 3.3.1 本地数据模型

```json
{
  "sessionId": "session_时间戳_随机串",
  "boardMode": "library",
  "boardId": 12,
  "boardName": "狼美人骑士",
  "playerCount": 12,
  "createTime": "2026-04-08T10:30:00.000Z",
  "updateTime": "2026-04-08T11:10:00.000Z",
  "records": [
    {
      "id": "rec_1",
      "type": "vote",
      "round": 1,
      "content": "1号、3号投给2号",
      "player": "1号、3号",
      "timestamp": "2026-04-08T10:35:00.000Z"
    }
  ]
}
```

字段说明：

- `boardMode`：
  - `library` 表示从板子库直接选择
  - `custom` 表示自定义板子
- `boardId`：
  - 仅当 `boardMode=library` 时保存选中的板子 ID

#### 3.3.2 笔记列表页

- 展示本地所有对局记录
- 每条卡片展示：
  - 板子名称
  - 人数
  - 记录条数
  - 最近更新时间
  - 最新一条记录摘要
- 右下角悬浮 `+` 号用于创建笔记
- 支持删除单条对局记录

#### 3.3.3 创建/编辑对局页

创建对局页支持两种模式：

1. `选择板子库`
2. `自定义板子`

其中：

- 选择板子库：
  - 调用后端 `/api/boards` 获取真实板子列表
  - 选择后自动带出板子名、人数、难度和概览信息
- 自定义板子：
  - 用户手填板子名称
  - 用户选择人数，当前支持 `6 / 9 / 12 / 15`
- 保存后进入对局详情页
- 再次编辑已有对局时，需按原有模式正确回填

#### 3.3.4 对局详情页

- 顶部显示：
  - 板子名称
  - 人数
  - 当前狼坑
- 时间线支持四类事件：
  - `seer`：预言家/查验记录
  - `vote`：投票记录
  - `speech`：发言记录
  - `wolfPack`：狼坑分析
- 底部工具栏固定入口：
  - 预言家
  - 投票
  - 发言
  - 狼坑
- 投票记录支持四种快捷模板：
  - 普通投票
  - 平票
  - 弃票
  - 警长归票
- 记录支持新增、编辑、删除
- 本地缓存需自动兼容旧结构并在读取时做归一化迁移

### 3.4 社区模块

#### 3.4.1 社区首页

- 展示帖子流
- 每个帖子卡片展示：
  - 用户头像
  - 昵称
  - 发布时间
  - 文本内容
  - 最多 3 张预览图
  - 点赞数与评论数
- 右下角悬浮 `+` 号用于发帖
- 支持下拉刷新

#### 3.4.2 发帖页

- 支持输入文本内容
- 支持最多 9 张图片
- 发布前需登录
- 发布接口需走内容安全适配层

#### 3.4.3 帖子详情页

- 展示帖子全文、全部图片、点赞、评论
- 支持评论发布
- 支持举报
- 支持本人删除自己的帖子

#### 3.4.4 社区治理闭环

- 用户侧可提交举报
- 后台侧可查看举报、处理举报、删除评论、下架帖子
- 举报处理需保留处理状态、处理人和处理时间

### 3.5 用户模块

#### 3.5.1 我的页面

- 采用黑金风格的个人中心布局
- 已登录状态展示：
  - 头像
  - 昵称
  - openid
  - 资料状态
- 未登录状态展示：
  - 微信登录按钮
  - 登录后社区互动能力说明
- 功能入口包括：
  - 我的收藏
  - 本地笔记
  - 设置

#### 3.5.2 微信登录与资料完善

- 登录通过 `uni.login` 获取 code，再请求后端 `/api/login`
- 登录成功后若资料仍为默认 mock 信息，则自动引导用户完善资料
- 资料完善弹层支持：
  - 通过 `chooseAvatar` 选择微信头像
  - 输入或选择微信昵称
  - 将头像上传到后端
  - 调用 `/api/user/info` 保存昵称与头像

#### 3.5.3 设置页

- 查看项目简介与版本信息
- 清理本地数据：
  - 收藏
  - 笔记
  - 登录态
  - 用户缓存

## 四、后端接口需求

### 4.1 统一响应格式

```json
{
  "code": 0,
  "msg": "success",
  "data": {}
}
```

分页接口统一返回：

```json
{
  "list": [],
  "total": 0,
  "page": 1,
  "size": 20
}
```

### 4.2 板子与角色接口

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/api/boards` | 分页查询板子列表 |
| GET | `/api/boards/{id}` | 查询板子详情 |
| GET | `/api/roles` | 查询角色列表 |
| GET | `/api/roles/{id}` | 查询角色详情 |

`/api/boards` 返回的卡片对象需至少包含：

- `id`
- `name`
- `playerCount`
- `difficulty`
- `tags`
- `coverImage`
- `briefConfig`
- `cardDescription`
- `campSummary`
- `lineupSummary`
- `roles`
- `featuredRoles`
- `summary`

说明：

- `featuredRoles` 用于小程序卡片角色头像标签展示
- 当前实现中展示数量上限为 7，并包含平民等数量型角色
- `summary` 用于后端兜底说明文案生成

### 4.3 用户接口

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/api/login` | 微信登录 |
| GET | `/api/user/info` | 获取用户资料 |
| PUT | `/api/user/info` | 更新用户昵称与头像 |

### 4.4 社区接口

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/api/posts` | 分页获取帖子列表 |
| GET | `/api/posts/{id}` | 获取帖子详情 |
| POST | `/api/posts` | 发布帖子 |
| POST | `/api/posts/{id}/like` | 点赞或取消点赞 |
| POST | `/api/comments` | 发表评论 |
| POST | `/api/reports` | 举报帖子或评论 |

### 4.5 管理后台接口

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/admin/login` | 管理员登录 |
| GET/POST/PUT/DELETE | `/admin/boards` | 板子管理 |
| GET/POST/PUT/DELETE | `/admin/roles` | 角色管理 |
| GET/PUT/DELETE | `/admin/posts` | 帖子治理 |
| GET/DELETE | `/admin/comments` | 评论治理 |
| GET/PUT | `/admin/reports` | 举报治理 |
| POST | `/admin/upload` | 图片上传 |

### 4.6 适配层要求

后端需保留以下可替换适配层，不允许将 mock 行为写死在 Controller 中：

- `AuthProvider`
- `ContentSafetyProvider`
- `UploadProvider`

开发阶段允许 mock-first：

- 微信登录：mock exchange code
- 内容安全：mock 审核通过
- 图片上传：本地磁盘落文件并返回 URL

## 五、数据库设计

### 5.1 当前核心表

- `boards`
- `roles`
- `board_roles`
- `users`
- `posts`
- `comments`
- `likes`
- `reports`
- `admin_users`

### 5.2 关键表结构要求

#### 5.2.1 boards

```sql
CREATE TABLE `boards` (
  `id` INT PRIMARY KEY AUTO_INCREMENT,
  `name` VARCHAR(100) NOT NULL,
  `player_count` TINYINT NOT NULL,
  `difficulty` VARCHAR(20) NOT NULL,
  `tags` JSON DEFAULT NULL,
  `cover_image` VARCHAR(500),
  `card_description` VARCHAR(255),
  `brief_config` VARCHAR(500) NOT NULL,
  `special_rules` JSON,
  `tips` JSON,
  `faqs` JSON,
  `win_condition` VARCHAR(100) DEFAULT '屠边',
  `rule_type` VARCHAR(50) DEFAULT '标准板',
  `status` TINYINT DEFAULT 1,
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

说明：

- `card_description` 为新增独立字段，专门用于板子列表卡片和板子详情概览的简介文案
- `brief_config` 为后台自动生成的阵容摘要

#### 5.2.2 roles

```sql
CREATE TABLE `roles` (
  `id` INT PRIMARY KEY AUTO_INCREMENT,
  `name` VARCHAR(50) NOT NULL UNIQUE,
  `alias` VARCHAR(50),
  `faction` VARCHAR(30) NOT NULL,
  `role_type` VARCHAR(30) NOT NULL,
  `camp` VARCHAR(60) NOT NULL,
  `skill` TEXT NOT NULL,
  `background` TEXT,
  `faqs` JSON,
  `portrait` VARCHAR(500),
  `full_illustration` VARCHAR(500),
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

字段约束：

- `faction` 固定为：`好人 / 狼人 / 第三方`
- `role_type` 固定为：`平民 / 神职 / 狼人 / 功能狼 / 第三方`
- `camp` 为展示态字段，例如：
  - `好人·神职`
  - `狼人·功能狼`
  - `第三方·第三方`

#### 5.2.3 reports

```sql
CREATE TABLE `reports` (
  `id` INT PRIMARY KEY AUTO_INCREMENT,
  `target_type` VARCHAR(20) NOT NULL,
  `target_id` INT NOT NULL,
  `openid` VARCHAR(100),
  `reason` VARCHAR(200),
  `process_status` VARCHAR(20) DEFAULT 'OPEN',
  `process_by` VARCHAR(100),
  `process_time` DATETIME,
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

说明：

- 举报必须保留处理状态、处理人、处理时间，形成后台治理闭环

## 六、管理后台需求

### 6.1 登录与总览

- 支持管理员账号密码登录
- 登录后进入黑金风格运营后台
- 首页展示板子数、角色数、帖子数、评论数、待处理举报数等摘要信息

### 6.2 板子管理

板子管理需支持：

- 列表展示：
  - 板子名称
  - 人数
  - 难度
  - 标签
  - 阵容摘要
- 新增/编辑板子：
  - 名称
  - 人数
  - 难度
  - 标签
  - 封面图
  - 列表简介 `cardDescription`
  - 胜利条件
  - 规则类型
  - 规则详情
  - 小贴士
  - FAQ
  - 角色动态分配
- 预览能力：
  - 阵容预览
  - `briefConfig` 预览
  - 卡片简介预览
- 保存时自动生成 `brief_config`

### 6.3 角色管理

- 角色管理必须使用固定枚举，禁止自由输入类型
- 当前录入维度：
  - 阵营 `faction`
  - 角色类型 `roleType`
- 支持维护：
  - 名称
  - 别名
  - 技能
  - 背景
  - FAQ
  - 头像
  - 立绘

### 6.4 社区治理

- 帖子列表查看
- 帖子状态调整
- 帖子删除
- 评论检索与删除
- 举报列表查看
- 举报处理状态修改
- 处理人、处理时间回显

### 6.5 图片上传

- 后台通过 `/admin/upload` 上传图片
- 返回图片 URL 后自动回填到表单中
- 当前实现以本地文件存储为主，保留后续切换对象存储能力

## 七、非功能需求

### 7.1 性能

- 板子列表首屏加载保持快速可用
- 列表图片需懒加载，滚动时不应明显卡顿
- 社区和板子列表需支持下拉刷新
- 长列表尽量控制首屏图片数量和请求体积

### 7.2 安全

- 管理后台使用 token 鉴权
- 普通用户接口通过登录态区分可读与可写能力
- 社区内容发布接入内容安全适配层
- 管理员密码需加密存储

### 7.3 可维护性

- 三端代码目录独立，可独立构建和启动
- 接口返回结构统一
- 后端 service 层不得依赖前端 mock 结构
- 小程序本地数据读取需兼容旧缓存并自动迁移

### 7.4 运行默认值

- 默认数据库：`wolfbook`
- 默认用户名：`root`
- 默认密码：`aa642353`
- 默认关闭自动执行 `schema.sql`，避免覆盖已存在表结构

## 八、验收标准

- 板子列表支持搜索、筛选、下拉刷新，卡片展示封面、人数徽标、角色头像标签和简介文案
- 板子卡片右上角爱心可完成收藏/取消收藏，并与“我的收藏”页同步
- 收藏页必须与板子列表使用同款卡片样式
- 板子详情“板型概览”优先展示后台配置的板子介绍
- 后台新增或修改 `cardDescription` 后，小程序列表和详情均能正确显示
- 角色图鉴按阵营展示，角色详情可查看技能、背景、FAQ 和相关板子
- 笔记创建页支持“选择板子库”与“自定义板子”两种模式，且能与后端板子列表联通
- 笔记时间线支持预言家、投票、发言、狼坑 4 类记录，投票至少支持普通投票、平票、弃票、警长归票
- 微信登录后可完善微信头像与昵称，并在“我的”页展示
- 社区支持发帖、评论、点赞、举报；后台可对帖子、评论、举报做治理
- 管理后台可完整增删改查板子和角色，并支持封面上传、角色配置、FAQ、小贴士维护
- 数据库结构、后端接口、小程序展示和后台字段保持一致，不允许前后端字段语义漂移

## 九、后续扩展方向

- AI 助手：板型推荐、局后复盘总结、发言提炼
- 电子法官：发牌、夜间流程推进、胜负判定
- 更完整的内容审核链路：真实微信安全接口与对象存储替换
- 更强的社区运营能力：精选板型、推荐流、活动位
